name: recipe_app
description: A new Flutter project.

publish_to: "none" # Remove this line if you want to publish to pub.dev.

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2 # untuk HTTP request
  provider: ^6.0.5
  cupertino_icons: ^1.0.8
  google_fonts: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
